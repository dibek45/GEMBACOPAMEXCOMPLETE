import { TipoAuditoriaPipe } from './tipo-auditoria.pipe';

describe('TipoAuditoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoAuditoriaPipe();
    expect(pipe).toBeTruthy();
  });
});

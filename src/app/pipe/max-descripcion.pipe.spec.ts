import { MaxDescripcionPipe } from './max-descripcion.pipe';

describe('MaxDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new MaxDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
